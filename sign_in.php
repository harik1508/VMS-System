<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>NIT Calicut</title>
  <!-- Global stylesheets -->
  <link href="https://fonts.googleapis.com/css?family=Roboto:400,300,100,500,700,900" rel="stylesheet" type="text/css">
  <link href="assets/css/icons/icomoon/styles.css" rel="stylesheet" type="text/css">
  <link href="assets/css/bootstrap.css" rel="stylesheet" type="text/css">
  <link href="assets/css/core.css" rel="stylesheet" type="text/css">
  <link href="assets/css/components.css" rel="stylesheet" type="text/css">
  <link href="assets/css/colors.css" rel="stylesheet" type="text/css">
  <link href="assets/css/Main.css" rel="stylesheet" type="text/css">
  <!-- /global stylesheets -->
  <!-- Core JS files -->

</head>


<body>

  <!-- Main navbar -->

  <!-- /main navbar -->


  <!-- Page container -->
  <div class="page-container Education ">

    <!-- Page content -->
    <div class="page-content">

      <!-- Main sidebar -->

      <!-- /main sidebar -->


      <!-- Main content -->
      <div class="content-wrapper">
        <div class="content-header">
          <div class="header-section">
            <div class="img">
              <img src="../NITC/assets/images/kud-logo.png" style="width: 490px">

            </div>
        
            

          </div>
        </div>
 
        <!-- Page header -->

        <!-- /page header -->


        <!-- Content area -->
        <div class="content">
          <div class="content-header02 ">
            <h4>Institute Management System
              </h4>
            <span>A NITCAA project sponsored by 1994, 1995, 1996 batches of Alumni</span>
          </div>
          <!-- <div class="headIcon">

            <a class="widget-image-container animation-fadeIn" href="#">
              <span class="widget-icon "><i><svg width="24" height="24" viewBox="0 0 24 24" fill="none"
                    xmlns="http://www.w3.org/2000/svg">
                    <g clip-path="url(#a)">
                      <path
                        d="M12 0a12 12 0 1 0 12 12A12.013 12.013 0 0 0 12 0Zm10 12a9.938 9.938 0 0 1-1.662 5.508l-1.192-1.193a.5.5 0 0 1-.146-.353V15a3 3 0 0 0-3-3h-3a1 1 0 0 1-1-1v-.5a.5.5 0 0 1 .5-.5A2.5 2.5 0 0 0 15 7.5v-1a.5.5 0 0 1 .5-.5h1.379a2.516 2.516 0 0 0 1.767-.732l.377-.377A9.969 9.969 0 0 1 22 12Zm-19.951.963 3.158 3.158A2.978 2.978 0 0 0 7.329 17H10a1 1 0 0 1 1 1v3.949a10.016 10.016 0 0 1-8.951-8.986ZM13 21.949V18a3 3 0 0 0-3-3H7.329a1 1 0 0 1-.708-.293l-4.458-4.458A9.978 9.978 0 0 1 17.456 3.63l-.224.224a.507.507 0 0 1-.353.146H15.5A2.5 2.5 0 0 0 13 6.5v1a.5.5 0 0 1-.5.5 2.5 2.5 0 0 0-2.5 2.5v.5a3 3 0 0 0 3 3h3a1 1 0 0 1 1 1v.962c.002.662.265 1.297.732 1.767l1.337 1.337A9.97 9.97 0 0 1 13 21.949Z"
                        fill="#374957" />
                    </g>
                    <defs>
                      <clipPath id="a">
                        <path fill="#fff" d="M0 0h24v24H0z" />
                      </clipPath>
                    </defs>
                  </svg></i></span>
            </a>

          </div> -->
         
          <ul class="grid-01">
            <li>
              <div class="box-panel">

                <a  href="https://ims.nitc.ac.in/Default/PageManager.html">
                  <div class="box-image">
                    <i><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 511.995 511.995" style="enable-background:new 0 0 511.995 511.995" xml:space="preserve" width="512" height="512"><path d="M346.999 511.995c-22.429.001-44.06-12.513-54.479-33.798l-30.156-56.463-17.107 32.654a19.956 19.956 0 0 1-1.489 2.411c-11.227 15.58-29.699 21.117-43.375 21.117l-.331-.001c-15.284-.101-30.503-6.659-41.752-17.995-11.683-11.772-18.116-27.399-18.116-44.005V156.961c0-23.619 13.308-44.471 34.729-54.419 21.218-9.854 45.475-6.722 63.469 8.149l131.747 98.111c8.859 6.597 10.692 19.127 4.096 27.986-6.598 8.859-19.127 10.692-27.986 4.095l-132.191-98.442a19.996 19.996 0 0 1-.959-.761c-9.047-7.64-18.61-4.12-21.328-2.859-2.716 1.261-11.576 6.296-11.576 18.14v258.955c0 12.887 10.609 21.938 20.132 22h.092c3.892 0 8.171-1.612 10.303-3.798l33.758-64.438a20 20 0 0 1 17.636-10.719h.08a20 20 0 0 1 17.642 10.578l48.151 90.155c.136.254.266.511.39.771 4.8 10.002 16.898 14.354 26.966 9.693a20.25 20.25 0 0 0 10.652-11.658c1.801-5.041 1.57-10.458-.641-15.31l-48.596-86.476a19.998 19.998 0 0 1 17.436-29.797h68.568c11.604-.593 16.237-9.731 17.387-12.539 1.18-2.879 4.385-12.958-3.896-21.741-7.577-8.037-7.205-20.694.831-28.272 8.037-7.578 20.695-7.204 28.272.832 16.41 17.405 20.936 42.059 11.809 64.342-9.076 22.16-29.471 36.477-53.225 37.364-.248.01-.497.014-.746.014h-34.819l32.196 57.293c.197.352.385.709.561 1.072 7.131 14.706 8.03 31.282 2.532 46.673-5.497 15.392-16.693 27.647-31.527 34.508a59.828 59.828 0 0 1-25.14 5.532zM162.808 78.542c9.686-5.311 13.231-17.468 7.92-27.153l-17-31c-5.311-9.685-17.466-13.23-27.152-7.919-9.686 5.311-13.231 17.468-7.92 27.153l17 31c3.638 6.634 10.486 10.387 17.554 10.387 3.25 0 6.548-.794 9.598-2.468zm-42.293 45.481c4.427-10.12-.188-21.913-10.307-26.34l-32-14c-10.12-4.428-21.912.188-26.34 10.307-4.427 10.12.188 21.913 10.307 26.34l32 14a19.922 19.922 0 0 0 8.006 1.682c7.705-.001 15.048-4.479 18.334-11.989zm124.651-64.692 11-33c3.493-10.479-2.17-21.806-12.649-25.298-10.475-3.495-21.806 2.17-25.298 12.649l-11 33c-3.493 10.479 2.17 21.806 12.649 25.298 2.098.7 4.23 1.032 6.327 1.032 8.373 0 16.178-5.301 18.971-13.681zm52.93 50.747 27-20c8.876-6.575 10.741-19.1 4.167-27.976-6.575-8.876-19.101-10.741-27.976-4.167l-27 20c-8.876 6.575-10.741 19.1-4.167 27.976 3.924 5.297 9.966 8.097 16.087 8.097 4.137 0 8.31-1.279 11.889-3.93zM79.62 222.576l30-12c10.256-4.102 15.244-15.742 11.142-25.997-4.103-10.257-15.744-15.245-25.997-11.142l-30 12c-10.256 4.102-15.244 15.742-11.142 25.997 3.128 7.82 10.638 12.578 18.575 12.578 2.473 0 4.986-.462 7.422-1.436z"/></svg>
                    </i>
                  </div>

                </a>
                
                <div class="box-content">

                  <h4 class="btn">Click Here</h4>
                 
                </div>


              </div>
            </li>
         
          </ul>


        </div>
        <!-- /content area -->

      </div>
      <!-- /main content -->

    </div>
    <!-- /page content -->

  </div>
  <!-- /page container -->

</body>
<script>
  import * as PIXI from "https://cdn.skypack.dev/pixi.js";
  import { KawaseBlurFilter } from "https://cdn.skypack.dev/@pixi/filter-kawase-blur";
  import SimplexNoise from "https://cdn.skypack.dev/simplex-noise@3.0.0";
  import hsl from "https://cdn.skypack.dev/hsl-to-hex";
  import debounce from "https://cdn.skypack.dev/debounce";

  // return a random number within a range
  function random(min, max) {
    return Math.random() * (max - min) + min;
  }

  // map a number from 1 range to another
  function map(n, start1, end1, start2, end2) {
    return ((n - start1) / (end1 - start1)) * (end2 - start2) + start2;
  }

  // Create a new simplex noise instance
  const simplex = new SimplexNoise();

  // ColorPalette class
  class ColorPalette {
    constructor() {
      this.setColors();
      this.setCustomProperties();
    }

    setColors() {
      // pick a random hue somewhere between 220 and 360
      this.hue = ~~random(220, 360);
      this.complimentaryHue1 = this.hue + 30;
      this.complimentaryHue2 = this.hue + 60;
      // define a fixed saturation and lightness
      this.saturation = 95;
      this.lightness = 50;

      // define a base color
      this.baseColor = hsl(this.hue, this.saturation, this.lightness);
      // define a complimentary color, 30 degress away from the base
      this.complimentaryColor1 = hsl(
        this.complimentaryHue1,
        this.saturation,
        this.lightness
      );
      // define a second complimentary color, 60 degrees away from the base
      this.complimentaryColor2 = hsl(
        this.complimentaryHue2,
        this.saturation,
        this.lightness
      );

      // store the color choices in an array so that a random one can be picked later
      this.colorChoices = [
        this.baseColor,
        this.complimentaryColor1,
        this.complimentaryColor2
      ];
    }

    randomColor() {
      // pick a random color
      return this.colorChoices[~~random(0, this.colorChoices.length)].replace(
        "#",
        "0x"
      );
    }

    setCustomProperties() {
      // set CSS custom properties so that the colors defined here can be used throughout the UI
      document.documentElement.style.setProperty("--hue", this.hue);
      document.documentElement.style.setProperty(
        "--hue-complimentary1",
        this.complimentaryHue1
      );
      document.documentElement.style.setProperty(
        "--hue-complimentary2",
        this.complimentaryHue2
      );
    }
  }

  // Orb class
  class Orb {
    // Pixi takes hex colors as hexidecimal literals (0x rather than a string with '#')
    constructor(fill = 0x000000) {
      // bounds = the area an orb is "allowed" to move within
      this.bounds = this.setBounds();
      // initialise the orb's { x, y } values to a random point within it's bounds
      this.x = random(this.bounds["x"].min, this.bounds["x"].max);
      this.y = random(this.bounds["y"].min, this.bounds["y"].max);

      // how large the orb is vs it's original radius (this will modulate over time)
      this.scale = 1;

      // what color is the orb?
      this.fill = fill;

      // the original radius of the orb, set relative to window height
      this.radius = random(window.innerHeight / 6, window.innerHeight / 3);

      // starting points in "time" for the noise/self similar random values
      this.xOff = random(0, 1000);
      this.yOff = random(0, 1000);
      // how quickly the noise/self similar random values step through time
      this.inc = 0.002;

      // PIXI.Graphics is used to draw 2d primitives (in this case a circle) to the canvas
      this.graphics = new PIXI.Graphics();
      this.graphics.alpha = 0.825;

      // 250ms after the last window resize event, recalculate orb positions.
      window.addEventListener(
        "resize",
        debounce(() => {
          this.bounds = this.setBounds();
        }, 250)
      );
    }

    setBounds() {
      // how far from the { x, y } origin can each orb move
      const maxDist =
        window.innerWidth < 1000 ? window.innerWidth / 3 : window.innerWidth / 5;
      // the { x, y } origin for each orb (the bottom right of the screen)
      const originX = window.innerWidth / 1.25;
      const originY =
        window.innerWidth < 1000
          ? window.innerHeight
          : window.innerHeight / 1.375;

      // allow each orb to move x distance away from it's x / y origin
      return {
        x: {
          min: originX - maxDist,
          max: originX + maxDist
        },
        y: {
          min: originY - maxDist,
          max: originY + maxDist
        }
      };
    }

    update() {
      // self similar "psuedo-random" or noise values at a given point in "time"
      const xNoise = simplex.noise2D(this.xOff, this.xOff);
      const yNoise = simplex.noise2D(this.yOff, this.yOff);
      const scaleNoise = simplex.noise2D(this.xOff, this.yOff);

      // map the xNoise/yNoise values (between -1 and 1) to a point within the orb's bounds
      this.x = map(xNoise, -1, 1, this.bounds["x"].min, this.bounds["x"].max);
      this.y = map(yNoise, -1, 1, this.bounds["y"].min, this.bounds["y"].max);
      // map scaleNoise (between -1 and 1) to a scale value somewhere between half of the orb's original size, and 100% of it's original size
      this.scale = map(scaleNoise, -1, 1, 0.5, 1);

      // step through "time"
      this.xOff += this.inc;
      this.yOff += this.inc;
    }

    render() {
      // update the PIXI.Graphics position and scale values
      this.graphics.x = this.x;
      this.graphics.y = this.y;
      this.graphics.scale.set(this.scale);

      // clear anything currently drawn to graphics
      this.graphics.clear();

      // tell graphics to fill any shapes drawn after this with the orb's fill color
      this.graphics.beginFill(this.fill);
      // draw a circle at { 0, 0 } with it's size set by this.radius
      this.graphics.drawCircle(0, 0, this.radius);
      // let graphics know we won't be filling in any more shapes
      this.graphics.endFill();
    }
  }

  // Create PixiJS app
  const app = new PIXI.Application({
    // render to <canvas class="orb-canvas"></canvas>
    view: document.querySelector(".orb-canvas"),
    // auto adjust size to fit the current window
    resizeTo: window,
    // transparent background, we will be creating a gradient background later using CSS
    transparent: true
  });

  // Create colour palette
  const colorPalette = new ColorPalette();

  app.stage.filters = [new KawaseBlurFilter(30, 10, true)];

  // Create orbs
  const orbs = [];

  for (let i = 0; i < 10; i++) {
    const orb = new Orb(colorPalette.randomColor());

    app.stage.addChild(orb.graphics);

    orbs.push(orb);
  }

  // Animate!
  if (!window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
    app.ticker.add(() => {
      orbs.forEach((orb) => {
        orb.update();
        orb.render();
      });
    });
  } else {
    orbs.forEach((orb) => {
      orb.update();
      orb.render();
    });
  }

  document
    .querySelector(".overlay__btn--colors")
    .addEventListener("click", () => {
      colorPalette.setColors();
      colorPalette.setCustomProperties();

      orbs.forEach((orb) => {
        orb.fill = colorPalette.randomColor();
      });
    });

</script>
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>


</html>